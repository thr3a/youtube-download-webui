- コーディング前に必ずpyproject.tomlを参照してライブラリ、Pythonバージョンなどを確認すること
- コードスタイルはruffのデフォルトルールに準拠すること
- 型注釈・docstringを活用すること
- サーバーはuv run uvicorn app.main:app --reload --host 0.0.0.0 --port 3000で起動中
- 本番環境ではDockerfileのイメージで起動する
